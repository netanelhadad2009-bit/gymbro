# ===== Push Notifications (APNs for iOS) =====
# Required for sending push notifications to iOS devices
# Get these from Apple Developer Console → Keys → Create APNs Key
#
# APNS_KEY_ID: The 10-character Key ID from Apple (e.g., ABC123DEFG)
APNS_KEY_ID=your_apns_key_id
#
# APNS_TEAM_ID: Your Apple Developer Team ID (found in Membership)
APNS_TEAM_ID=your_team_id
#
# APNS_KEY_P8: The contents of your .p8 private key file
# Include the full key including BEGIN/END lines, or just the base64 content
# For Vercel: paste the entire key content as a single line
APNS_KEY_P8=-----BEGIN PRIVATE KEY-----\nYOUR_KEY_CONTENT_HERE\n-----END PRIVATE KEY-----
#
# APNS_BUNDLE_ID: Your app's bundle identifier
APNS_BUNDLE_ID=com.fitjourney.app

# ===== Web Push (VAPID Keys) =====
# Required for sending push notifications to web browsers
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# ===== Cron Job Security =====
# Secret token to authorize cron job requests
# Generate with: openssl rand -hex 32
# Vercel automatically provides this when cron jobs are configured
CRON_SECRET=your_cron_secret_here

# ===== Coach Composer Feature Flag =====
# Controls whether the message composer (input + send button) appears on the Coach screen.
#
# DEFAULT: Composer is HIDDEN (disabled)
#   - Leave this unset or set to anything other than 'true'/'1'
#
# To ENABLE (show) the composer:
#   - Set to 'true' or '1' explicitly
#
# Example to enable composer:
# NEXT_PUBLIC_COACH_COMPOSER_ENABLED=true
#
# Example to disable (or just leave it unset/commented):
# NEXT_PUBLIC_COACH_COMPOSER_ENABLED=false

# ===== FatSecret API Credentials =====
# Used as fallback when Open Food Facts doesn't have a product
# Get credentials at: https://platform.fatsecret.com/api/
#
# REQUIRED for barcode lookup fallback feature
FATSECRET_CLIENT_ID=your_client_id_here
FATSECRET_CLIENT_SECRET=your_client_secret_here

# ===== Israeli Ministry of Health Nutrition Data =====
# Provides nutrition data for Israeli products (729 barcode prefix)
# Data source: https://data.gov.il/
#
# Mode: "dataset" (fetch from data.gov.il) or "api" (if official API available)
ISRAEL_MOH_MODE=dataset
# Data.gov.il resource URL (update resource_id as needed)
ISRAEL_MOH_DATA_URL=https://data.gov.il/api/3/action/datastore_search?resource_id=c3cb0630-0650-46c1-a91f-8eae7e3e7b9e&limit=50000
# Cron schedule for refreshing dataset (every 6 hours)
ISRAEL_MOH_REFRESH_CRON=0 */6 * * *

# ===== Legal Links (Required for App Store Compliance) =====
# These URLs must be publicly accessible HTTPS links
# Required by Apple App Review Guidelines 3.1.2 and 5.1.1
#
# IMPORTANT: Set these to your actual legal pages before submitting to App Store
# The links will appear on the /premium paywall screen
#
# Example values (replace with your actual URLs):
# NEXT_PUBLIC_PRIVACY_POLICY_URL=https://fitjourney.app/privacy
# NEXT_PUBLIC_TERMS_URL=https://fitjourney.app/terms
NEXT_PUBLIC_PRIVACY_POLICY_URL=https://example.com/privacy
NEXT_PUBLIC_TERMS_URL=https://example.com/terms

# ===== Email Notifications (Resend) =====
# Used to send admin notification emails (e.g., new subscriptions)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_example_key
