/**
 * Layout for nutrition scan flow
 * Removes bottom nav and padding for full-screen experience
 */
export default function ScanLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
